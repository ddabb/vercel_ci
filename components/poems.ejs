<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>诗作列表</title>
</head>
<body>
    <h1>诗作列表</h1>
    <ul>
        <% poems.forEach(poem => { %>
            <li>
                <h2><%= poem.name %></h2>
                <p><%= poem.contents.join('') %></p>
                <p>作者：<%= poem.poetName %></p>
            </li>
        <% }) %>
    </ul>
</body>
</html>